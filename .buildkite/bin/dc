#!/bin/bash -ie

DOCKER_COMPOSE_FILE=${DOCKER_COMPOSE_FILE:-docker-compose.buildkite.yml}
PROJECT_NAME=${PROJECT_NAME:-"$BUILDKITE_PIPELINE_SLUG$BUILDKITE_BUILD_NUMBER"}

docker-compose -f $DOCKER_COMPOSE_FILE -p $PROJECT_NAME $@
