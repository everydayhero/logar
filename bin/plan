#!/bin/bash -ie

CONFIG_ARGS=()
PLAN_ARGS=()

for arg in "$@"; do
  if [[ $arg == -backend* ]]; then
    CONFIG_ARGS+=($arg)
  else
    PLAN_ARGS+=($arg)
  fi
done

terraform remote config ${CONFIG_ARGS[@]}
terraform plan ${PLAN_ARGS[@]}
